/* ══════════════════════════════════════════════════════════════
   前端配置 —— 部署时改这一个文件。
   这里没有密钥，也不需要密钥：密钥只存在后端 config.yaml 里，
   浏览器永远接触不到大模型 API。
   ══════════════════════════════════════════════════════════════ */

// 后端 API 地址。
//
//   同域部署（Nginx 把 /api 反代到后端）→ 保持 '/api/v1'，不用改。
//   前后端分域部署                        → 改成后端完整地址，例如
//                                          'https://api.ddi.example.com/api/v1'
//
// 分域时必须同时把后端 config.yaml 的 api.cors_origins 设成前端的域名，
// 否则浏览器会因跨域策略拦下请求。
window.DDI_API_BASE = '/api/v1';
